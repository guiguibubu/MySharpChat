<UserControl x:Class="MySharpChat.Client.GUI.ConnectionUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MySharpChat.Client.GUI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="WhiteSmoke">
        <StackPanel Grid.Column="0" Grid.Row="0"
                    Grid.ColumnSpan="1" Grid.RowSpan="1"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Center"
                    Margin="10">
            <Label>Username</Label>
            <TextBox x:Name="UserNameInputBox" Margin="10"/>
            <Label>IP Address</Label>
            <TextBox x:Name="IpInputBox" Margin="10"/>
            <Label>Port</Label>
            <TextBox x:Name="PortInputBox" Margin="10"/>
            <Button x:Name="ConnectButton" Margin="10" Padding="10,2" HorizontalAlignment="Center">Connect</Button>
            <TextBlock x:Name="ConnectionStatus" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center" Margin="0,20"/>
        </StackPanel>
    </Grid>
</UserControl>
